/**
 * 
 */
 
    date = new Date();
    var thisMonth = date.toLocaleString();
    
    date.setMonth(date.getMonth() - 1);
    var aMonth = date.toLocaleString();
   
    
    date.setMonth(date.getMonth() - 1);
    var twoMonth = date.toLocaleString();
    
    date.setMonth(date.getMonth() - 1);
    var threeMonth = date.toLocaleString();
    
    date.setMonth(date.getMonth() - 1);
    var fourMonth = date.toLocaleString();
    
    date.setMonth(date.getMonth() - 1);
    var fiveMonth = date.toLocaleString();
    
    date.setMonth(date.getMonth() - 1);
    var sixMonth = date.toLocaleString();
    
    date.setMonth(date.getMonth() - 1);
    var sevenMonth = date.toLocaleString();
    
    date.setMonth(date.getMonth() - 1);
    var eightMonth = date.toLocaleString();
    
    date.setMonth(date.getMonth() - 1);
    var nineMonth = date.toLocaleString();
    
    date.setMonth(date.getMonth() - 1);
    var tenMonth = date.toLocaleString();
    
     date.setMonth(date.getMonth() - 1);
    var eleMonth = date.toLocaleString();
 
